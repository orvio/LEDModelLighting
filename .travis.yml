os: linux
language: c

branches:
  only:
    - main

addons:
  apt:
    packages:
      - doxygen

script:
  - doxygen Doxyfile

deploy:
  provider: pages
  cleanup: false
  local_dir: docs/html
  github_token: $GH_REPO_TOKEN
  strategy: git
  on:
    branch: main
